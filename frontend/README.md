# Frontend

This directory contains the React frontend for the Campus Marketplace application.

## Running the Application

To run the frontend, navigate to this directory and run the following commands:

```
npm install
npm start
```

This will start the development server and open the application in your default browser.
